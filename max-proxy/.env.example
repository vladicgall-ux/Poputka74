# Публичный адрес основного сервера (Нидерланды) — сюда форвардятся все запросы.
# Обязательно с https://, без слэша на конце.
UPSTREAM_URL=https://bot1234.bothost.tech

# Порт, который слушает прокси (bothost подставит свой автоматически — обычно 3000).
PORT=3000
